# REFLECTION.md

## Overview

The assignment was a good exercise to get familiar with scripting, and what the process dealing with setting it up and working in a real world setting. Messing up syntax and having to look up documentation. Or look where things went rock. Also beyond the scope of the assignment but learning how to use regex expressions to do some filtering to get the integer.

### reflections / improvements

Also beyond the scope of the class. But I learned that there is switch-case in bash that can be useful!!
also I needed to add `>&2`. This prints to stderr rather than stdout. dSO that we added on the end of all of the echo'd outputs that exits 1. Also, using $(()), and the difference between $(), $(()) and \`\`. and the difference between them. 
